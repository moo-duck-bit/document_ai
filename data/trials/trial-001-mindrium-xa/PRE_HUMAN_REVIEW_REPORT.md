# Trial 1 — 구현 검증 (동결)

> **Trial 1 동결: `true`**  
> 유형: 구현 검증(Implementation Validation)  
> Harness 코드 · `generated_patch_preserving` DOCX · 재실행 — **금지**  
> 본 문서는 결과 기록 전용입니다.  
> 갱신: `2026-07-18T06:00:00Z`

`generated_patch_preserving/` = **파이프라인 실행 증거**로만 인용합니다.

---

## 종합 결과

**실패(FAILED)**

### 사유

**수용 기준 미충족(Acceptance Criteria Not Met)**

(수용 기준은 상태값이 아니라 실패 사유입니다.)

---

## 통과(PASS)

### 1. 요구사항 매핑

**근거**
- 자연어 변경요청(CR)이 Req.6으로 올바르게 매핑됨.

### 2. 패치 보존

**근거**
- 원본 문서를 통째로 재생성하지 않고 패치를 적용함.

### 3. 원본 보존

**근거**
- 원본 DOCX가 보존됨.
- 원본 유지형(patch-in-place) 전략이 정상 수행됨.

### 4. 문서 구조 보존

**근거**
- 원본 구조와 서식이 유지됨.

---

## 실패(FAIL)

### 1. 의미적 일관성

**근본 원인**
- 설명(description) 셀만 패치됨. 제목·목적·기준은 기존 ‘인증 오류 UX’인데 CR은 계정 잠금 정책이라 의미가 충돌함.

**개선 방향**
- Trial 2: 의미 검색 + 일관성 게이트(다중 필드 갱신 또는 매핑 재선택).

### 2. 요구사항 영향 식별

**근본 원인**
- 정확한 ID(Exact-ID)에만 의존. 본문 의미 검색 없음. IA-07 경유 형제 Req 확장 없음(Req.105 누락).

**개선 방향**
- Trial 2: 검색 기반 후보 발견 + 그래프 확장.

### 3. 설계 전파

**근본 원인**
- MDDR 미패치. 설계 영향이 식별되지 않음(`식별되지 않음` ≠ `불필요`).

**개선 방향**
- Trial 2: 검색된 MDDR 블록을 패치하거나, 문서화된 사유와 함께 명시적 생략. 보존과 전파 지표를 분리.

---

## 근본 원인 요약

- Exact-ID 의존
- 의미 검색(semantic retrieval) 부재
- 그래프 확장 부재

---

## Trial 1 동결

Trial 1은 여기서 종료합니다.

- 구현·생성 결과 추가 수정 없음
- `generated_patch_preserving`은 파이프라인 실행 증거로만 인용
- 재실행·DOCX 덮어쓰기·Harness 수정 금지

---

## Trial 2 목표

- 검색 보강 의미 검색(retrieval-augmented semantic retrieval)
- 영향 후보 발견
- 요구사항 간 영향 분석
- 설계 문서 전파
- 검증 문서 전파
- 재검증

---

## Trial 2 수용 기준

(Trial 2 시작 전 성공 기준)

- ✓ Req.105가 영향 후보로 검색·포함됨
- ✓ 의미적 일관성 검증을 통과함
- ✓ 갱신된 요구사항 필드 간 모순이 없음
- ✓ MDDR이 올바르게 패치됨 **또는** 문서화된 사유와 함께 명시적으로 생략됨
- ✓ 문서 간 전파 추적(trace)이 생성됨
